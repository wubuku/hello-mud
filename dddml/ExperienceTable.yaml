singletonObjects:
  ExperienceTable:
    properties:
      Levels:
        itemType: ExperienceLevel
        # isList: true
        tableName: XpTableLevel
        # Default table name is "ExperienceTableLevel" which is too long.
        # TODO: throw if "tableName" is too long.
      ReservedBool1:
        type: bool
