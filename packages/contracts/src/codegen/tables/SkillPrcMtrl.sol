// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct SkillPrcMtrlData {
  uint32 productionMaterialItemId;
  uint32 productionMaterialQuantity;
}

library SkillPrcMtrl {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "app", name: "SkillPrcMtrl", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x74626170700000000000000000000000536b696c6c5072634d74726c00000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0008020004040000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (uint8, uint256, uint8, uint64)
  Schema constant _keySchema = Schema.wrap(0x002a0400001f0007000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint32, uint32)
  Schema constant _valueSchema = Schema.wrap(0x0008020003030000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](4);
    keyNames[0] = "skillProcessIdSkillType";
    keyNames[1] = "skillProcessIdPlayerId";
    keyNames[2] = "skillProcessIdSequenceNumber";
    keyNames[3] = "productionMaterialIndex";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](2);
    fieldNames[0] = "productionMaterialItemId";
    fieldNames[1] = "productionMaterialQuantity";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get productionMaterialItemId.
   */
  function getProductionMaterialItemId(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (uint32 productionMaterialItemId) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get productionMaterialItemId.
   */
  function _getProductionMaterialItemId(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (uint32 productionMaterialItemId) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Set productionMaterialItemId.
   */
  function setProductionMaterialItemId(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialItemId
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((productionMaterialItemId)), _fieldLayout);
  }

  /**
   * @notice Set productionMaterialItemId.
   */
  function _setProductionMaterialItemId(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialItemId
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((productionMaterialItemId)), _fieldLayout);
  }

  /**
   * @notice Get productionMaterialQuantity.
   */
  function getProductionMaterialQuantity(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (uint32 productionMaterialQuantity) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get productionMaterialQuantity.
   */
  function _getProductionMaterialQuantity(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (uint32 productionMaterialQuantity) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Set productionMaterialQuantity.
   */
  function setProductionMaterialQuantity(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialQuantity
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((productionMaterialQuantity)), _fieldLayout);
  }

  /**
   * @notice Set productionMaterialQuantity.
   */
  function _setProductionMaterialQuantity(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialQuantity
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((productionMaterialQuantity)), _fieldLayout);
  }

  /**
   * @notice Get the full data.
   */
  function get(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (SkillPrcMtrlData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal view returns (SkillPrcMtrlData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialItemId,
    uint32 productionMaterialQuantity
  ) internal {
    bytes memory _staticData = encodeStatic(productionMaterialItemId, productionMaterialQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    uint32 productionMaterialItemId,
    uint32 productionMaterialQuantity
  ) internal {
    bytes memory _staticData = encodeStatic(productionMaterialItemId, productionMaterialQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    SkillPrcMtrlData memory _table
  ) internal {
    bytes memory _staticData = encodeStatic(_table.productionMaterialItemId, _table.productionMaterialQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex,
    SkillPrcMtrlData memory _table
  ) internal {
    bytes memory _staticData = encodeStatic(_table.productionMaterialItemId, _table.productionMaterialQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  ) internal pure returns (uint32 productionMaterialItemId, uint32 productionMaterialQuantity) {
    productionMaterialItemId = (uint32(Bytes.getBytes4(_blob, 0)));

    productionMaterialQuantity = (uint32(Bytes.getBytes4(_blob, 4)));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   *
   *
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths,
    bytes memory
  ) internal pure returns (SkillPrcMtrlData memory _table) {
    (_table.productionMaterialItemId, _table.productionMaterialQuantity) = decodeStatic(_staticData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    uint32 productionMaterialItemId,
    uint32 productionMaterialQuantity
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(productionMaterialItemId, productionMaterialQuantity);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint32 productionMaterialItemId,
    uint32 productionMaterialQuantity
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(productionMaterialItemId, productionMaterialQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(
    uint8 skillProcessIdSkillType,
    uint256 skillProcessIdPlayerId,
    uint8 skillProcessIdSequenceNumber,
    uint64 productionMaterialIndex
  ) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](4);
    _keyTuple[0] = bytes32(uint256(skillProcessIdSkillType));
    _keyTuple[1] = bytes32(uint256(skillProcessIdPlayerId));
    _keyTuple[2] = bytes32(uint256(skillProcessIdSequenceNumber));
    _keyTuple[3] = bytes32(uint256(productionMaterialIndex));

    return _keyTuple;
  }
}
