// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct ShipInventoryData {
  uint32 inventoryItemId;
  uint32 inventoryQuantity;
}

library ShipInventory {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "app", name: "ShipInventory", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x7462617070000000000000000000000053686970496e76656e746f7279000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0008020004040000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (uint256, uint64)
  Schema constant _keySchema = Schema.wrap(0x002802001f070000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint32, uint32)
  Schema constant _valueSchema = Schema.wrap(0x0008020003030000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](2);
    keyNames[0] = "shipId";
    keyNames[1] = "inventoryIndex";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](2);
    fieldNames[0] = "inventoryItemId";
    fieldNames[1] = "inventoryQuantity";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get inventoryItemId.
   */
  function getInventoryItemId(uint256 shipId, uint64 inventoryIndex) internal view returns (uint32 inventoryItemId) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get inventoryItemId.
   */
  function _getInventoryItemId(uint256 shipId, uint64 inventoryIndex) internal view returns (uint32 inventoryItemId) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Set inventoryItemId.
   */
  function setInventoryItemId(uint256 shipId, uint64 inventoryIndex, uint32 inventoryItemId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((inventoryItemId)), _fieldLayout);
  }

  /**
   * @notice Set inventoryItemId.
   */
  function _setInventoryItemId(uint256 shipId, uint64 inventoryIndex, uint32 inventoryItemId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((inventoryItemId)), _fieldLayout);
  }

  /**
   * @notice Get inventoryQuantity.
   */
  function getInventoryQuantity(
    uint256 shipId,
    uint64 inventoryIndex
  ) internal view returns (uint32 inventoryQuantity) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get inventoryQuantity.
   */
  function _getInventoryQuantity(
    uint256 shipId,
    uint64 inventoryIndex
  ) internal view returns (uint32 inventoryQuantity) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Set inventoryQuantity.
   */
  function setInventoryQuantity(uint256 shipId, uint64 inventoryIndex, uint32 inventoryQuantity) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((inventoryQuantity)), _fieldLayout);
  }

  /**
   * @notice Set inventoryQuantity.
   */
  function _setInventoryQuantity(uint256 shipId, uint64 inventoryIndex, uint32 inventoryQuantity) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((inventoryQuantity)), _fieldLayout);
  }

  /**
   * @notice Get the full data.
   */
  function get(uint256 shipId, uint64 inventoryIndex) internal view returns (ShipInventoryData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(uint256 shipId, uint64 inventoryIndex) internal view returns (ShipInventoryData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(uint256 shipId, uint64 inventoryIndex, uint32 inventoryItemId, uint32 inventoryQuantity) internal {
    bytes memory _staticData = encodeStatic(inventoryItemId, inventoryQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(uint256 shipId, uint64 inventoryIndex, uint32 inventoryItemId, uint32 inventoryQuantity) internal {
    bytes memory _staticData = encodeStatic(inventoryItemId, inventoryQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(uint256 shipId, uint64 inventoryIndex, ShipInventoryData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.inventoryItemId, _table.inventoryQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(uint256 shipId, uint64 inventoryIndex, ShipInventoryData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.inventoryItemId, _table.inventoryQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(bytes memory _blob) internal pure returns (uint32 inventoryItemId, uint32 inventoryQuantity) {
    inventoryItemId = (uint32(Bytes.getBytes4(_blob, 0)));

    inventoryQuantity = (uint32(Bytes.getBytes4(_blob, 4)));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   *
   *
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths,
    bytes memory
  ) internal pure returns (ShipInventoryData memory _table) {
    (_table.inventoryItemId, _table.inventoryQuantity) = decodeStatic(_staticData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(uint256 shipId, uint64 inventoryIndex) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(uint256 shipId, uint64 inventoryIndex) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(uint32 inventoryItemId, uint32 inventoryQuantity) internal pure returns (bytes memory) {
    return abi.encodePacked(inventoryItemId, inventoryQuantity);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint32 inventoryItemId,
    uint32 inventoryQuantity
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(inventoryItemId, inventoryQuantity);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(uint256 shipId, uint64 inventoryIndex) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(shipId));
    _keyTuple[1] = bytes32(uint256(inventoryIndex));

    return _keyTuple;
  }
}
